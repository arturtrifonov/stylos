# Overlays

Status: Partial
Scope: Modality, dismissal, stacking, positioning, scroll lock.

A dialog returns focus where BEH-FOCUS-09 says it does.

### BEH-OVERLAYS-01 — A modal traps focus

**MUST.** While a modal overlay is open, Tab cycles inside it.

Why: focus escaping a modal leaves a person editing a form they cannot see.
