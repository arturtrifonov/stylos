# Hand-edited. This file is the source for its component's structural data.
# The CSV import that created it ran once, on 2026-08-20, and will not run
# again — see docs/components/registry/README.md for the schema and workflow.

id: "Table"
name: "Table"
level: "layout"
role: "container"
flow_behavior:
  - "fill"
children:
  - "Table Row Head"
  - "Table Row Body"
parents:
  - "Accordion / Container"
  - "Bottom Sheet"
  - "Flex Layout"
  - "Header"
  - "Hero"
  - "Modal"
  - "Side Panel"
  - "Side Panel Menu"
notes: "children are the two rows rather than the cells: a cell is allowed in a row and a row is allowed in a table, and nothing places a cell in a table directly."
import:
  batch: 1
  ready: false
