# Contract written 2026-09-03 from the live component set. The children/parents
# lists and the import block are carried over from the CSV import of 2026-08-20.
# See docs/components/registry/README.md for the schema.
#
# 2026-09-02: the entry left button/ and its id lost the slash group — a slash
# group is not how this registry expresses a family, see the registry README,
# *Families*. 2026-09-03: "Button Basic" became "Button Base", matching the
# component in Figma and the compound name naming.md §2 gives as its own example.

id: "Button Base"
name: "Button Base"
family: "Button"
level: "object"
role: "trigger"
html: "<button type=\"button\">"
status: "draft"
version: "0.1"
summary: "The filled action button — the one a view is built around."
purpose: "Every screen has an action it exists to let someone take, and that action has to be found before it is read. This is the member of the family that fills its box with its tone and knocks the label out of it, so it is located by scanning a screen rather than by reading one. That weight is the whole of what separates it from its siblings: the box, the run of sizes, the label, the two icon slots and the states are the same in all three, and choosing between them is choosing how loudly the action should speak, not what it does."
use_when:
  - "The action is what the view, form, dialog or row is for — save, create, submit, confirm, apply, delete."
  - "Two or more actions sit together and one of them should be reached for first, without the reader comparing them to find out which."
do_not_use_when:
  -
    text: "The action supports the main one rather than being it. Two filled buttons side by side compete, and the reader settles the competition by reading both."
    instead:
      - "Button Outline"
      - "Button Ghost"
  -
    text: "The control is a local action on the component it sits in — clear a field, remove a tag, dismiss a toast. Those take an icon's space and show their affordance under the pointer; this takes a button's space and is always visible."
    instead: "Button Inner"
  -
    text: "The control is icon-only and stands on its own — a toolbar action, an action in a header. That is an object with its own size run, not this one with its label emptied."
    instead:
      - "Button Icon Base"
      - "Button Icon Outline"
      - "Button Icon Ghost"
  -
    text: "The control navigates rather than acts. A link drawn as a button is still a link, and drawing it this way breaks the middle click, the copy-address, and the back button."
    instead: "Link"
  -
    text: "Pressing it leaves a state behind rather than performing an action — selecting, toggling, expanding."
    instead:
      - "Toggle Label"
      - "Checkbox Label"
children:
  - "Badge"
  - "Avatar"
  - "Icon"
  - "Indicator Status"
  - "Indicator Special"
  - "Tag Fill"
  - "Tag Outline"
  - "Loader"
parents:
  - "Accordion"
  - "Alert"
  - "Toast"
  - "Bottom Sheet"
  - "Flex Layout"
  - "Header"
  - "Hero"
  - "Modal"
  - "Side Panel"
  - "Side Panel Menu"
uses:
  - "Icon"
flow_behavior:
  - "hug"
a11y:
  -
    status: "requires"
    note: "The label is uppercased by a transform, not typed in capitals — the characters stay as they were written. An implementation has to do the same and change the case in CSS, because typing the label in capitals changes the accessible name as well as the appearance, and a screen reader may then spell it out letter by letter."
  -
    status: "requires"
    note: "The label has to say what the action does. This component supplies visual weight and nothing else; a button labelled OK inside a filled box is still a button labelled OK."
sizing_model:
  horizontal: "hug"
  vertical: "fixed"
  adjustable: true
  intent: "The height is authored on the Object run and the width is whatever the label needs, so a button is a band of a known height wrapping a variable phrase. The label neither wraps nor truncates — it has no width to truncate against — so a phrase too long for its column widens the button rather than being cut. The type run is a component-specific mapping rather than the Object profile: one step below it at every size, which typography.md allows where it is documented, and this is that documentation. A button carries two or three words beside the content it acts on, and at the profile's own measures it would out-shout that content. Padding and gap have their own runs; the radius does not move with size, because a button reads as the same shape at every size rather than a shape that rounds off as it grows. Width is the consumer's to name where a layout calls for it — a named width is still fixed, not fill."
  sizes:
    -
      size: "extra small"
      box: "size/s-3_000"
      padding_inline: "gap/g-1_500"
      gap: "gap/g-0_500"
      font_size: "size/0_750"
      line_height: "line height/string/0_750"
      icon: "size/s-2_000"
    -
      size: "small"
      box: "size/s-4_000"
      padding_inline: "gap/g-2_000"
      gap: "gap/g-0_625"
      font_size: "size/0_875"
      line_height: "line height/string/0_875"
      icon: "size/s-2_500"
    -
      size: "medium"
      box: "size/s-5_000"
      padding_inline: "gap/g-2_500"
      gap: "gap/g-0_750"
      font_size: "size/1_125"
      line_height: "line height/string/1_125"
      icon: "size/s-3_000"
    -
      size: "large"
      box: "size/s-6_000"
      padding_inline: "gap/g-3_000"
      gap: "gap/g-0_875"
      font_size: "size/1_375"
      line_height: "line height/string/1_375"
      icon: "size/s-3_500"
    -
      size: "extra large"
      box: "size/s-7_000"
      padding_inline: "gap/g-3_500"
      gap: "gap/g-1_000"
      font_size: "size/1_625"
      line_height: "line height/string/1_625"
      icon: "size/s-4_000"
api:
  -
    name: "tone"
    kind: "variant"
    default: "primary"
    description: "Which meaning the action carries. The tone fills the box and the label is knocked out of it in the inverted text role, so every tone reads at the same weight and none of them recedes. A tone is a colour role, not an outcome: a destructive action is danger because destroying is what it does, not because anything went wrong."
    values:
      -
        value: "base"
        note: "No meaning of its own. The filled button for an action that is the main one on its surface without being brand or consequence."
      -
        value: "primary"
        note: "The default, and the brand. One per surface — a second one is two main actions, which is none."
      -
        value: "warning"
        note: "The action has a consequence the reader should weigh first, and can undo afterwards."
      -
        value: "danger"
        note: "The action destroys something, or cannot be undone. Not an error — the button is working exactly as intended."
      -
        value: "success"
        note: "Rare on a trigger. For a button that completes something rather than starting it."
  -
    name: "size"
    kind: "variant"
    default: "medium"
    description: "The band height, and with it the label measure, the padding, the gap and the icon slots. Sized to the density of the surface the button sits on rather than to the importance of the action — importance is what tone is for."
    values:
      -
        value: "extra small"
        note: "For a dense row — a table, a toolbar. At 24 tall the button is well under the 44 by 44 pointer target WCAG 2.2 asks for; the system has not chosen an accessibility target, so this is recorded as a measurement rather than judged."
      -
        value: "small"
      -
        value: "medium"
        note: "The default, and what most surfaces want."
      -
        value: "large"
      -
        value: "extra large"
        note: "For a button that is the only thing on its surface — an empty state, a landing section."
  -
    name: "state"
    kind: "variant"
    default: "default"
    description: "The pointer condition. Each step darkens the tone rather than changing it, so the button stays the colour it started as while it is pressed. disabled drops the shadow as well as the colour, because an inert control should not appear to sit above the surface. Focus is not a value here — it is a separate boolean, because a button can be focused and hovered at once and a variant axis cannot say so."
    values:
      -
        value: "default"
      -
        value: "hover"
      -
        value: "active"
      -
        value: "disabled"
        note: "Every tone resolves into the same neutral surface, which is deliberate — a disabled control is structurally inert regardless of what it would have meant enabled; see color.md. The label sits at 1.44:1 against it, low enough to read as unavailable."
  -
    name: "has leading icon"
    kind: "boolean"
    default: "false"
    description: "Shows a mark before the label. Off by default: the label names the action, and a mark in front is for the cases where the action has a symbol the reader already knows."
    controls:
      - "leading icon"
  -
    name: "leading icon"
    kind: "instance"
    default: "Placeholder"
    description: "The mark shown before the label. It qualifies the action rather than naming it, so a button whose meaning rests on the mark alone is a button with the wrong label."
  -
    name: "has trailing icon"
    kind: "boolean"
    default: "false"
    description: "Shows a mark after the label. This is the side for a mark that points outward — that the action opens something, continues somewhere, or reveals more."
    controls:
      - "trailing icon"
  -
    name: "trailing icon"
    kind: "instance"
    default: "Placeholder"
    description: "The mark shown after the label, on the same terms as the leading one."
  -
    name: "label text"
    kind: "text"
    default: "Button"
    description: "What the action does, as a verb or a verb phrase, in two or three words. The label neither wraps nor truncates, so a longer phrase widens the button until it breaks the layout around it — which is the signal to shorten the phrase, not to constrain the button."
  -
    name: "is focused"
    kind: "boolean"
    default: "false"
    description: "Shows the focus ring — a two-pixel outline in the primary role, sitting outside the button and clearing its corner exactly. A boolean rather than a state value because focus coincides with the pointer conditions instead of replacing them: a button under the pointer and under the keyboard is both at once. It does nothing while state is disabled, where the ring does not exist."
limitations:
  - "The focus ring is absent from every disabled variant, so is focused has no effect there. That is the intended reading — a disabled control is not in the tab order — rather than a property that silently fails."
  - "The base tone measures 4.55:1 against its own label in light mode, clearing 4.5:1 by less than rounding. Any move of surface/bold/base/default or text/inverted is a contrast change, not a colour change."
  - "The label cannot wrap. There is no two-line button and no truncating one; the component is one line by construction."
  - "The focus ring does not scale with size. A 24-tall button and a 56-tall one take the same two-pixel ring at the same offset — deliberate, because the ring belongs to the keyboard rather than to the size run."
figma_notes:
  - "Both icon containers are hidden by their booleans in every one of the 100 variants, so only their bindings could be read; their geometry when shown is unverified."
  - "The icon containers bind their width to the local icon size collection rather than to the dimension collection, so the value is not resolvable by npm run tokens:report. That collection exists for optical compensation of the icon's overhang and is a deliberate per-component setting."
  - "The focus ring is an instance of a shared Focus component that has no registry entry of its own. Its stroke weight is the single unbound value on this component — a raw 2 where everything else resolves to a token."
  - "The api block above is ordered for readability, per naming.md §8 and §10. Figma's API returns properties in a key order that does not match the panel, so the order the designer sees could not be read and is not asserted here."
figma:
  file_key: "WUc07ZBtjRvypXtsOlbVut"
  node_id: "4479-13507"
  last_verified: "2026-09-03"
notes: "The accessibility target is not chosen yet — PLAN.md Stage 5 — so contrast and target-size measurements are recorded as facts rather than judged against a criterion the system has not adopted."
import:
  batch: 1
  ready: true
