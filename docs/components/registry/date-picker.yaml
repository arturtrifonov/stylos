# Hand-edited. This file is the source for its component's structural data.
# The CSV import that created it ran once, on 2026-08-20, and will not run
# again — see docs/components/registry/README.md for the schema and workflow.

id: "Date Picker"
name: "Date Picker"
level: "object"
role: "toolbar"
flow_behavior:
  - "absolute"
  - "fixed"
notes: ""
import:
  batch: 2
  ready: false
