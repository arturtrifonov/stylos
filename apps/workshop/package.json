{
  "name": "stylos-workshop",
  "version": "0.1.0",
  "private": true,
  "description": "Storybook workshop for @stylos/ui. Stories are generated from the registry (tools/build-ui-stories.mjs) — run npm run ui:generate at the root first.",
  "type": "module",
  "scripts": {
    "dev": "storybook dev -p 6006 --no-open",
    "build": "storybook build"
  },
  "devDependencies": {
    "@storybook/addon-svelte-csf": "^5.1.3",
    "@storybook/svelte": "^10.6.0",
    "@storybook/svelte-vite": "^10.6.0",
    "@stylos/ui": "*",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "storybook": "^10.6.0",
    "svelte": "^5.46.4",
    "vite": "^8.0.0"
  },
  "license": "UNLICENSED"
}
